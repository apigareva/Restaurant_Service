import { Layout } from '../layout/layout.jsx'
import { Restaurants } from '../restaurants/restaurants.jsx';

export const App = () => {
    return (
        <Layout>
            <Restaurants />
        </Layout>
    );
}

